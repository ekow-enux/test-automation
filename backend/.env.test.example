# Test Environment Configuration
NODE_ENV=test

# MongoDB Atlas Configuration for Testing
# Replace with your MongoDB Atlas connection string
# Example: mongodb+srv://username:password@cluster.mongodb.net/testdb?retryWrites=true&w=majority
MONGODB_TEST_URI=mongodb+srv://username:password@cluster.mongodb.net/testdb?retryWrites=true&w=majority

# JWT Configuration (use a different secret for testing)
JWT_SECRET=test-jwt-secret-key-for-testing-only

# Client URL for CORS configuration
CLIENT_URL=http://localhost:3000

# Server Configuration
PORT=5000

# Optional: Logging level for tests
LOG_LEVEL=error